from app.browser_automation.base import ApplicationAdapter


class GreenhouseApplicationAdapter(ApplicationAdapter):
    name = "greenhouse"
    # Greenhouse hosts the application form at the posting URL itself.
